# Backend dependencies
docker>=7.0
fastapi>=0.110
uvicorn[standard]>=0.29
redis>=5.0
hiredis>=2.0
python-dotenv>=1.0

# Optional / sponsor integrations (safe to install; code degrades without config)
sentry-sdk[fastapi]>=2.0

# Added later:
#   redisvl        # RedisVL agent memory (beyond-caching Redis use)
#   anthropic      # agent_engine (Claude agents)
#   uagents        # Fetch.ai wrapper
